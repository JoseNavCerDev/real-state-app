header(class="bg-indigo-600 p-4") 
    div(class="container mx-auto flex justify-between items-center")
        a(href="/api/public/welcome")
            h1(class="text-2xl text-white font-extrabold text-center") Real
                span(class="font-normal") State
            
        nav(class="my-5 text-sm md:flex md:items-center md:gap-3 font-bold text-white")
            a(href="/api/properties/my-properties") My properties
            a(href="") My profile

            form(action="/api/user/log-out" method="POST") 
                input(type="hidden" name="_csrf" value=csrfToken)
                input(class="bg-indigo-800 hover:bg-indigo-900 py-2 px-10 rounded" type="submit" value="Log out")
